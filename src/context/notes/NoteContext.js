// context will store our states and functions and we will make them accessible for all components.
import { createContext } from "react";

const noteContext = createContext();

export default noteContext;